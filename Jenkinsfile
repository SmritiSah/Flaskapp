pipeline {
    agent {
        Dockerfile true
    }

    stages {
        stage('Cloning Git') {
            steps {
                sh 'echo checking out source code'
            }
        }
    }
}
